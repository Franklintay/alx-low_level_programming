#include "main.h"

/**
 * add - add two integers and return the result
 * @x: one of the integers
 * @y: the other integer
 * Return: Result of the addition
 */

int add(int x, int y)
{
	return (x + y);
}
